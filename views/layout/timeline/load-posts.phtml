<div id="posts" data-story-user="<?php echo $carovl['profile']['user_id']; ?>">
	<?php 
	$stories = getPosts(array(
		'filter_by' => 'all',
		'publisher_id' => $carovl['profile']['user_id']
	));
	if (count($stories) == 0) {
		echo loadPage('timeline/no-stories');
	} else {
		foreach ($stories as $carovl['story']) {
			echo loadPage('story/content');
		}
	}
	?>
</div>
<?php if (count($stories) > 1 && $carovl['logged_in'] == true) { ?> 
<div class="card load-more">
	<div class="card-block">
		<div class="get-more-posts pointer" id="get-more-posts" onclick="getMorePosts();">
			<span>
				<i class="fa fa-repeat icon-loader" data-icon="repeat"></i>
			</span>
		</div>
	</div>
</div>
<?php } ?>